{
  "private": true,
  "scripts": {
    "start": "tsx start.ts",
    "deploy": "tsx deploy.ts",
    "execute": "tsx execute.ts",
    "build": "rm -rf artifacts && npx hardhat clean && npx hardhat compile && cd call-contract/hello-world && mxpy contract build"
  },
  "devDependencies": {
    "tsx": "^3.12.7",
    "typescript": "^5.0.4",
    "@axelar-network/axelar-local-dev": "3.0.0",
    "@axelar-network/axelarjs-sdk": "^0.12.8",
    "@multiversx/sdk-core": "^12.5.0"
  }
}
